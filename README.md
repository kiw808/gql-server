# GraphQL Server

## Basic GraphQL server API

### Installation

-   Clone this repo
-   Install dependencies
    ```
    $ npm install
    ```
-   Start the server
    ```
    $ node server2.js
    ```
-   Open `localhost:5000/graphql` in your browser
